# vagrant-sample

Simple HTTP server with simple URL.

## Usage

Install plugin.

```sh
$ vagrant plugin install vagrant-hostsupdater
```

Start.

```sh
$ vagrant up
```

Open. (OS X)

```sh
$ open http://example.com
```
